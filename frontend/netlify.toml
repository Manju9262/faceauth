# Netlify configuration for ZepIris React SPA
[build]
  publish = "dist"
  command = "npm run build"

# Route all requests to index.html for Single Page App routing support
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
